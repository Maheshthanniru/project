#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run linting (allow warnings)
npm run lint || true

# Run type checking (allow failures)
npm run type-check || true

# Run formatting check
npm run format:check

# Run security audit (allow failures)
npm run security:audit || true 